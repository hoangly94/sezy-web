

import React from 'react';
import { IconIProps, IconWrapper } from 'sezy-design/components/icon';

const Logo = (props: IconIProps) => {
    return IconWrapper(undefined, props,
        <svg viewBox="0 0 39 15">
        </svg>
    )
}

export default Logo
